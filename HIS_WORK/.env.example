# ── OpenAI — AI offer text generation ─────────────────────────────────────────
# Get your key at: platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# ── Mapbox — 3D map in the frontend ───────────────────────────────────────────
MAPBOX_TOKEN=pk.eyJ1...

# ── VAPID — Web Push Notifications ────────────────────────────────────────────
# Generate once with: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print(v.public_key.decode()); print(v.private_key.decode())"
# Or use: https://web-push-codelab.glitch.me/
VAPID_PUBLIC_KEY=BK...
VAPID_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
VAPID_EMAIL=mailto:you@example.com
